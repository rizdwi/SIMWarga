<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/spacing_md"
    android:layout_marginVertical="@dimen/spacing_xs"
    app:cardBackgroundColor="@color/surface"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/spacing_md">

        <!-- Gender indicator icon -->
        <ImageView
            android:id="@+id/ivGender"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="@string/hint_jenis_kelamin"
            android:src="@drawable/ic_person"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/primary" />

        <TextView
            android:id="@+id/tvNamaLengkap"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_md"
            android:layout_marginEnd="@dimen/spacing_sm"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_subtitle"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/btnEditAnggota"
            app:layout_constraintStart_toEndOf="@id/ivGender"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvNik"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_caption"
            app:layout_constraintEnd_toStartOf="@id/btnEditAnggota"
            app:layout_constraintStart_toStartOf="@id/tvNamaLengkap"
            app:layout_constraintTop_toBottomOf="@id/tvNamaLengkap" />

        <TextView
            android:id="@+id/tvHubungan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:background="@drawable/ic_launcher_background"
            android:backgroundTint="@color/chip_bg"
            android:paddingHorizontal="@dimen/spacing_sm"
            android:paddingVertical="2dp"
            android:textColor="@color/chip_text"
            android:textSize="@dimen/text_caption"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="@id/tvNamaLengkap"
            app:layout_constraintTop_toBottomOf="@id/tvNik" />

        <TextView
            android:id="@+id/tvRincian"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_caption"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/tvNamaLengkap"
            app:layout_constraintTop_toBottomOf="@id/tvHubungan" />

        <!-- Actions -->
        <ImageButton
            android:id="@+id/btnEditAnggota"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/edit_kk"
            android:src="@drawable/ic_edit"
            app:layout_constraintEnd_toStartOf="@id/btnHapusAnggota"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/primary" />

        <ImageButton
            android:id="@+id/btnHapusAnggota"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/hapus"
            android:src="@drawable/ic_delete"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/error" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
